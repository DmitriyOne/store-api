import React, { FC } from 'react'

import { IProduct } from '@interfaces'

interface IProps {
	product: IProduct
}

export const ProductCard: FC<IProps> = ({ product }) => {

	return (
		<>
		</>
	)
}
