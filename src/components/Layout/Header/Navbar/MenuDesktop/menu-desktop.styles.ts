import { StackProps } from '@chakra-ui/react'

export const hStackStyles: StackProps = {
	spacing: 8,
	alignItems: 'center',
}

export const navStyles: StackProps = {
	spacing: 8,
	alignItems: 'center',
	display: { base: 'none', md: 'flex' },
}

export const ulStyles: StackProps = {
	spacing: 4,
}
